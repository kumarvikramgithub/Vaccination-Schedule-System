import { SortPatientAgePipe } from './sort-patient-age.pipe';

describe('SortPatientAgePipe', () => {
  it('create an instance', () => {
    const pipe = new SortPatientAgePipe();
    expect(pipe).toBeTruthy();
  });
});
